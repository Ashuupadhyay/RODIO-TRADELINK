const express = require("express");

const router = express.Router();

const {
  adminUpdateReview,
  adminDeleteReview,
} = require("../controllers/adminreviewapi");

// ==========================================
// ADMIN REVIEW ROUTES
// ==========================================

// Edit review
router.put("/reviews/:id", adminUpdateReview);

// Delete review
router.delete("/reviews/:id", adminDeleteReview);

module.exports = router;